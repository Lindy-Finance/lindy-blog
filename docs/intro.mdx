---
sidebar_position: 1
---

# Início

Vamos explorar como investir em cryptomoedas. **Diretamente na sua carteira dentro da rede blockchain (on chain)**.

Blog posts support [Docusaurus Markdown features](https://docusaurus.io/docs/markdown-features), such as [MDX](https://mdxjs.com/).

:::tip

Use the power of React to create interactive blog posts.

```js
<button onClick={() => alert('button clicked!')}>Click me!</button>
```

<button onClick={() => alert('button clicked!')}>Click me!</button>

:::


Separamos em tópicos voce pular algum tema em que você já entenda sobre os conceitos que iremos abordar.

- O que é a blockchain Conflux?
- Quais os diferenciais da Conflux?
- Qual a projeção de valorização?
- O que é renda passiva em criptomoedas?
- Como fazer staking de $CFX?
- Comparação de stakings na $CFX
- Quais as diferenças em fazer staking na Lindy
- Como fazer staking na Lindy?
- O que é o bitcoin?
- Qual a previsão do Bitcoin para 2025 e 2035?
- O que é uma rede blockchain?
- Qual a diferença de uma blockchain Proof of Work (PoW) e Proof of Stake (PoS)?
- O que é um endereço de carteira?
- O que é uma carteira digital?
- O que é o Ethereum?
- O que é um smart contract?
- O que é uma uma corretora centralizada (CEX)?
- O que é uma uma corretora descentralizada (DEX)?
- O que é uma piscina de liquidez?
- O que é Staking? 
-->


Get started by **creating a new account Lindy**.

Or **try Docusaurus immediately** with **[docusaurus.new](https://docusaurus.new)**.

### O que você irá precisar

- [Node.js](https://nodejs.org/en/download/) version 16.14 or above:
  - When installing Node.js, you are recommended to check all checkboxes related to dependencies.

## Criar conta na Lindy Finance

Generate a new Docusaurus site using the **classic template**.

The classic template will automatically be added to your project after you run the command:

```bash
npm init docusaurus@latest my-website classic
```

You can type this command into Command Prompt, Powershell, Terminal, or any other integrated terminal of your code editor.

The command also installs all necessary dependencies you need to run Docusaurus.

## Fazer o cadastro KYC

Run the development server:

```bash
cd my-website
npm run start
```

## Comprar os tokens CFX via PIX direto para sua carteira


The `cd` command changes the directory you're working with. In order to work with your newly created Docusaurus site, you'll need to navigate the terminal there.

## Ativar o Staking dos tokens CFX


The `cd` command changes the directory you're working with. In order to work with your newly created Docusaurus site, you'll need to navigate the terminal there.

## Acompanhamento, manutenção e suporte


The `cd` command changes the directory you're working with. In order to work with your newly created Docusaurus site, you'll need to navigate the terminal there. -->
